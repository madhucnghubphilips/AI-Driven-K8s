# EKS Security Notes

Enable EKS control plane logging for audit, API, authenticator, controller manager, and scheduler logs. Restrict or disable public endpoint access and use private endpoint access for managed workloads.

Configure Kubernetes secrets encryption with a customer-managed KMS key. Review IAM roles for service accounts and keep IAM policies scoped to the exact AWS actions and resources needed by each workload.
